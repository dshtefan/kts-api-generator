package jetprofile.rest.customers

/*import jetprofile.dao.Customer
import jetprofile.dao.Team*/

object CustomersPresenter {
    /*fun displayDetails(customer: Customer): CustomersModel.Details = CustomersModel.Details(
        id = customer.id.value,
        name = customer.name,
        code = customer.code,
        country = customer.country.name,
        type = customer.profileType
    )

    fun displayTeam(team: Team): CustomersModel.Team = CustomersModel.Team(
        id = team.id.value,
        name = team.name
    )*/
}
