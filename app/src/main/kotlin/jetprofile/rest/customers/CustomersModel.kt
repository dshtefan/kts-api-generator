package jetprofile.rest.customers

/*import jetprofile.db.ProfileType
import jetprofile.json.JsonSerializable*/

object CustomersModel {
    /*object Requests {
        data class Search(val name: String, val id: Int?, val code: Int?) : JsonSerializable
    }

    object Results {
        data class Search(val customers: List<Details>) : JsonSerializable
        data class Teams(val teams: List<Team>) : JsonSerializable
    }

    data class Details(
        val id: Int,
        val name: String,
        val code: Long,
        val country: String,
        val type: ProfileType
    ) : JsonSerializable

    data class Team(
        val id: Int,
        val name: String
    ) : JsonSerializable*/
}

